{
  "name": "land-layout-planner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --format=esm --target=es2020 --outfile=dist/app.js",
    "watch": "esbuild src/main.ts --bundle --format=esm --target=es2020 --outfile=dist/app.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
